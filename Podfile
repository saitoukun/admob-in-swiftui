# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'admob-in-swiftui' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for admob-in-swiftui

  target 'admob-in-swiftuiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'admob-in-swiftuiUITests' do
    # Pods for testing
  end
  
  # AdMob
  pod 'Google-Mobile-Ads-SDK'

end
